# 📴 Offline-режим приложения

Полное руководство по работе приложения без подключения к интернету.

---

## ✨ Что работает offline

### Полностью доступно:
✅ Просмотр ранее загруженных страниц  
✅ Навигация по приложению  
✅ Чтение сохранённых профилей  
✅ Просмотр кэшированных изображений  
✅ Использование сохранённых фильтров  
✅ Чтение истории сообщений  

### Работает с ограничениями:
⚠️ Создание новых записей (сохраняется в очередь)  
⚠️ Отправка сообщений (отправятся при восстановлении связи)  
⚠️ Обновление профиля (применится при подключении)  

### Не работает offline:
❌ Telegram Payments (требует подключение)  
❌ Загрузка новых изображений  
❌ Получение новых уведомлений  
❌ Обновление списка пользователей онлайн  

---

## 🔧 Технические детали

### Service Worker
Файл: `/public/sw.js`

**Кэширование:**
- **Статические файлы**: HTML, CSS, JS (постоянно)
- **Изображения**: до 100 изображений в кэше
- **API запросы**: до 50 последних запросов (5 минут)
- **Unsplash изображения**: 30 дней в кэше

**Стратегии:**
- `CacheFirst` - для изображений (сначала кэш, потом сеть)
- `NetworkFirst` - для API (сначала сеть, потом кэш)
- `NetworkOnly` - для критичных операций

### Детектор подключения
Файл: `src/utils/offlineDetector.ts`

**Функции:**
- Мониторинг статуса подключения каждые 30 секунд
- Событие `online` / `offline` от браузера
- Проверка доступности через `fetch('/favicon.ico')`
- Уведомление подписчиков об изменении статуса

### Индикатор offline
Компонент: `src/components/OfflineIndicator.tsx`

**Поведение:**
- 🔴 Красная полоса вверху при потере связи
- 🟢 Зелёная полоса на 3 секунды при восстановлении
- Автоматически скрывается когда онлайн

### Очередь запросов
Hook: `src/hooks/useOfflineStorage.ts`

**Принцип работы:**
1. Пользователь совершает действие offline
2. Запрос добавляется в `localStorage` очередь
3. При восстановлении связи все запросы отправляются автоматически
4. Неудачные попытки остаются в очереди

**Методы:**
```typescript
const { isOnline, queueSize, addToQueue, processQueue, clearQueue } = useOfflineStorage();

// Добавить в очередь
addToQueue('/api/like', 'POST', { userId: 123 });

// Обработать очередь вручную
processQueue();

// Очистить очередь
clearQueue();
```

---

## 📱 Использование

### Регистрация Service Worker
Автоматически при загрузке страницы в `index.html`:

```javascript
if ('serviceWorker' in navigator) {
  window.addEventListener('load', () => {
    navigator.serviceWorker.register('/sw.js');
  });
}
```

### Проверка статуса подключения
```typescript
import { offlineDetector } from '@/utils/offlineDetector';

// Получить текущий статус
const isOnline = offlineDetector.getStatus();

// Подписаться на изменения
const unsubscribe = offlineDetector.subscribe((online) => {
  console.log('Connection status:', online ? 'online' : 'offline');
});

// Отписаться
unsubscribe();
```

### Добавление запросов в очередь
```typescript
import { useOfflineStorage } from '@/hooks/useOfflineStorage';

const Component = () => {
  const { isOnline, queueSize, addToQueue } = useOfflineStorage();

  const handleAction = async () => {
    if (!isOnline) {
      addToQueue('/api/action', 'POST', { data: 'value' });
      toast({ title: 'Действие сохранено', description: 'Отправится при подключении' });
      return;
    }

    // Обычная отправка
    await fetch('/api/action', { method: 'POST', body: JSON.stringify({ data: 'value' }) });
  };

  return (
    <div>
      {!isOnline && <p>Offline режим. В очереди: {queueSize} запросов</p>}
      <button onClick={handleAction}>Выполнить действие</button>
    </div>
  );
};
```

---

## 🔍 Проверка работы

### Chrome DevTools:
1. Откройте DevTools (F12)
2. Вкладка **Application** → **Service Workers**
3. Увидите зарегистрированный SW
4. Вкладка **Network** → поставьте галочку **Offline**
5. Обновите страницу — она загрузится из кэша

### Проверка кэша:
1. DevTools → **Application** → **Cache Storage**
2. Увидите 3 кэша:
   - `static-v1` - статика
   - `dynamic-v1` - динамические запросы
   - `images-v1` - изображения

### Тестирование очереди:
1. Откройте DevTools → **Application** → **Local Storage**
2. Найдите ключ `offline_queue`
3. Выключите интернет
4. Совершите действие (лайк, сообщение)
5. Включите интернет — запросы уйдут автоматически

---

## 🚀 Оптимизация

### Размер кэша
Файл `sw.js`, константа `MAX_CACHE_SIZE = 100`:
- Можно увеличить для больших приложений
- Уменьшить для экономии памяти

### Время кэширования API
```javascript
// В sw.js, для API-запросов
caches.open(DYNAMIC_CACHE).then(cache => {
  cache.put(request, response);
  // Срок жизни: 5 минут (300 секунд)
});
```

### Приоритет кэширования
Изменить стратегию в `sw.js`:
```javascript
// CacheFirst - быстро, но может быть устаревшим
// NetworkFirst - всегда свежее, но медленнее offline
// NetworkOnly - только сеть (для критичных данных)
```

---

## 🐛 Отладка

### Service Worker не регистрируется
- Проверьте консоль на ошибки
- SW работает только на HTTPS или localhost
- Убедитесь что файл `/sw.js` доступен

### Кэш не обновляется
- Измените версию кэша: `CACHE_NAME = 'love-is-v2'`
- SW автоматически удалит старый кэш

### Очередь не обрабатывается
- Откройте DevTools → Console
- Проверьте вызывается ли `processQueue()`
- Убедитесь что `isOnline === true`

### Индикатор не показывается
- Компонент должен быть в `Index.tsx`
- Проверьте импорт `OfflineIndicator`
- Убедитесь что CSS анимации работают

---

## 📊 Мониторинг

### Логи Service Worker
Открыть: `chrome://serviceworker-internals/`

### Статистика кэша
```javascript
// В консоли браузера
caches.keys().then(keys => {
  keys.forEach(key => {
    caches.open(key).then(cache => {
      cache.keys().then(requests => {
        console.log(key, requests.length);
      });
    });
  });
});
```

### Размер localStorage
```javascript
// В консоли браузера
let total = 0;
for (let key in localStorage) {
  if (localStorage.hasOwnProperty(key)) {
    total += localStorage[key].length + key.length;
  }
}
console.log('localStorage size:', (total / 1024).toFixed(2), 'KB');
```

---

## ✅ Лучшие практики

1. **Всегда проверяйте `isOnline` перед критичными операциями**
2. **Показывайте пользователю статус подключения**
3. **Добавляйте запросы в очередь при offline**
4. **Уведомляйте о восстановлении связи**
5. **Не кэшируйте чувствительные данные**
6. **Регулярно очищайте устаревший кэш**
7. **Тестируйте на реальных условиях с плохим интернетом**

---

**Готово!** Ваше приложение теперь работает offline! 📴✨

При возникновении проблем проверьте логи в DevTools или обратитесь в поддержку.
