# Система оплаты и бронирования

## Обзор системы

Комплексная система оплаты с депозитными счетами, поддержкой множества валют и криптовалют, а также эскроу-механизмом для безопасных сделок.

---

## 1. Депозитная система (Кошелек)

### Поддерживаемые валюты:
- **Фиатные**: RUB (₽), USD ($), EUR (€)
- **Криптовалюты**: BTC, ETH, USDT

### Функционал кошелька:
- ✅ Пополнение баланса в любой валюте
- ✅ Вывод средств на карты/кошельки
- ✅ Мультивалютные счета
- ✅ Автоматическое зачисление (1-10 мин)
- ✅ Обработка выводов (1-24 часа)

### Компоненты:
- `WalletCard` - отображение балансов
- `DepositModal` - пополнение
- `WithdrawModal` - вывод средств

---

## 2. Процесс бронирования

### Шаг 1: Создание запроса (Покупатель)
Покупатель выбирает:
- Дату и время встречи
- Длительность (1-12 часов)
- Валюту оплаты
- Комментарий (опционально)

**Условия:**
- На балансе должна быть полная сумма
- Средства НЕ списываются сразу

**Статус:** `pending_seller_confirmation`

---

### Шаг 2: Таймер подтверждения (15 минут)
Продавец получает уведомление и имеет **15 минут** на ответ.

**Продавец может:**
- ✅ Подтвердить → переход к шагу 3
- ❌ Отклонить → бронирование отменяется

**Если время истекло:** автоматическая отмена

---

### Шаг 3: Эскроу (после подтверждения)
После подтверждения продавцом:
- ✅ Средства **списываются** с баланса покупателя
- ✅ Зачисляются на **эскроу-счет** (заморожены)
- ✅ Недоступны обеим сторонам до завершения

**Статус:** `confirmed`

---

### Шаг 4: Готовность сторон
**В назначенное время:**

1. **Продавец нажимает "Готова"**
   - Статус: `seller_ready`
   - Ожидание покупателя

2. **Покупатель нажимает "Готов"**
   - Статус: `buyer_ready` → `in_progress`
   - **Запускается таймер встречи**

---

### Шаг 5: Таймер встречи
После готовности обеих сторон начинается обратный отсчет:
- ⏱️ Отображается: `ЧЧ:ММ:СС`
- 📊 Progress bar (% оставшегося времени)
- ⚠️ Предупреждение при < 10 минут

**Особенности:**
- Таймер идет в реальном времени
- Обновляется каждую секунду
- При истечении времени → встреча завершается

---

### Шаг 6: Продление встречи
**Кнопка "Продлить" доступна если:**
- ✅ Встреча в статусе `in_progress`
- ✅ На балансе достаточно средств

**Варианты продления:**
- +1 час
- +2 часа
- +3 часа

**Действие:**
- Списание с баланса
- Добавление времени к таймеру
- Пересчет общей стоимости

**Если баланса НЕ хватает:**
- ⚫ Кнопка серая (disabled)
- ⚠️ Уведомление "Пополните баланс"

---

## 3. Статусы бронирований

| Статус | Описание | Кто может действовать |
|--------|----------|----------------------|
| `pending_seller_confirmation` | Ожидает ответа продавца (15 мин) | Продавец |
| `confirmed` | Подтверждено, деньги на эскроу | Обе стороны |
| `seller_ready` | Продавец готова к встрече | Покупатель |
| `buyer_ready` | Покупатель готов | Автоматический переход |
| `in_progress` | Встреча идет, таймер активен | Обе стороны (продление) |
| `completed` | Встреча завершена | - |
| `cancelled` | Отменено | - |
| `rejected` | Отклонено продавцом | - |

---

## 4. Эскроу-механизм

### Принцип работы:
1. **Подтверждение** → средства замораживаются
2. **Встреча** → средства на эскроу
3. **Завершение** → перевод продавцу

### Защита сторон:
- 🛡️ Покупатель: возврат при отмене продавцом
- 🛡️ Продавец: гарантия оплаты после встречи
- 🛡️ Платформа: комиссия удерживается автоматически

---

## 5. Интерфейс компонентов

### BookingConfirmationModal
**Для продавца:**
- Таймер обратного отсчета (15:00)
- Детали встречи
- Кнопки: "Подтвердить" / "Отклонить"

**Для покупателя:**
- Статус ожидания
- Таймер до истечения
- Информация о встрече

### ActiveBookingModal
**Статусы готовности:**
- Иконки продавца и покупателя
- Индикация "Готов" / "Ожидание"
- Стрелка между ними

**Таймер встречи:**
- Крупный счетчик времени
- Progress bar
- Цвет меняется при < 10 мин (красный)

**Продление:**
- 3 кнопки: +1ч, +2ч, +3ч
- Показ стоимости под каждой
- Активность зависит от баланса

### ImprovedBookingModal
**Создание бронирования:**
- Выбор даты/времени
- Длительность (1-12ч)
- Выбор валюты (отображение балансов)
- Расчет итоговой суммы
- Проверка достаточности средств
- Инструкция по процессу

---

## 6. Типы данных

```typescript
interface Wallet {
  balances: WalletBalance[];
}

interface WalletBalance {
  currency: Currency;
  amount: number;
  symbol: string;
}

interface Booking {
  id: number;
  serviceId: number;
  serviceName: string;
  sellerId: number;
  sellerName: string;
  buyerId: number;
  buyerName: string;
  date: string;
  time: string;
  duration: number; // часы
  pricePerHour: number;
  totalPrice: number;
  currency: Currency;
  status: BookingStatus;
  createdAt: string;
  confirmedAt?: string;
  sellerReadyAt?: string;
  buyerReadyAt?: string;
  startedAt?: string;
  completedAt?: string;
  expiresAt?: string; // Для таймера подтверждения
  remainingTime?: number; // Оставшееся время в секундах
  escrowAmount?: number;
}
```

---

## 7. Утилиты (bookingManager.ts)

### Функции управления:
- `createBooking()` - создание запроса
- `confirmBooking()` - подтверждение продавцом
- `rejectBooking()` - отклонение
- `sellerReady()` - готовность продавца
- `buyerReady()` - готовность покупателя (→ старт таймера)
- `extendBooking()` - продление встречи
- `completeBooking()` - завершение
- `cancelBooking()` - отмена

### Проверки:
- `canExtendBooking()` - можно ли продлить
- `isBookingExpired()` - истек ли таймер подтверждения
- `getBookingStatusLabel()` - текст и цвет для статуса

---

## 8. Интеграция в профиль

**В ProfilePage добавлено:**
- ✅ `WalletCard` - управление балансами
- ✅ `DepositModal` - пополнение
- ✅ `WithdrawModal` - вывод
- ✅ Отображение активных бронирований
- ✅ История завершенных встреч

**Расположение:**
1. VIP статус (если есть)
2. **Кошелек** (новое)
3. График работы (для продавцов)
4. Бронирования

---

## 9. Безопасность

### Эскроу гарантии:
- Средства заморожены до завершения
- Невозможно отменить после старта таймера
- Автовозврат при отклонении/отмене

### Таймауты:
- 15 минут на подтверждение продавцом
- Автоотмена при пропуске
- Возврат средств покупателю

### Валидация:
- Проверка баланса перед созданием
- Проверка перед продлением
- Блокировка при недостаточных средствах

---

## 10. Пользовательский опыт

### Уведомления:
- 🔔 Новое бронирование (продавец)
- ⏰ Таймер истекает (обе стороны)
- ✅ Подтверждено
- ❌ Отклонено/Отменено
- 💰 Средства зачислены (продавец после завершения)

### Цветовая индикация:
- 🟡 Ожидание - желтый
- 🔵 Подтверждено - синий
- 🟢 В процессе - зеленый
- 🔴 Отклонено/Мало времени - красный
- ⚫ Завершено - серый

---

## Примеры использования

### Создание бронирования:
```tsx
<ImprovedBookingModal
  isOpen={showBooking}
  onClose={() => setShowBooking(false)}
  serviceName="VIP сопровождение"
  sellerName="Анастасия"
  pricePerHour={25000}
  userBalances={wallet.balances}
  onCreateBooking={handleCreateBooking}
/>
```

### Управление активной встречей:
```tsx
<ActiveBookingModal
  isOpen={showActive}
  onClose={() => setShowActive(false)}
  booking={currentBooking}
  userRole="buyer"
  userBalance={walletBalance}
  onSellerReady={handleSellerReady}
  onBuyerReady={handleBuyerReady}
  onExtend={handleExtend}
/>
```

---

Система полностью интегрирована и готова к использованию! 🚀
