# История транзакций - Руководство

## Обзор

Полная система учета всех финансовых операций пользователя с расширенной фильтрацией, статистикой и возможностью экспорта.

---

## Типы транзакций

### 1. Пополнение (deposit)
**Описание:** Внесение средств на счет

**Когда создается:**
- Пользователь пополняет баланс через `DepositModal`
- Мгновенное зачисление (в демо)

**Атрибуты:**
```typescript
{
  type: 'deposit',
  amount: 50000,
  currency: 'RUB',
  status: 'completed',
  description: 'Пополнение счета RUB'
}
```

**Иконка:** ↓ (ArrowDownToLine)  
**Цвет:** Зеленый

---

### 2. Вывод средств (withdraw)
**Описание:** Снятие средств со счета

**Когда создается:**
- Пользователь выводит деньги через `WithdrawModal`
- Указывает адрес карты/кошелька

**Атрибуты:**
```typescript
{
  type: 'withdraw',
  amount: 10000,
  currency: 'RUB',
  status: 'pending', // → 'completed' через 1-24 часа
  description: 'Вывод средств на 1234 5678...'
}
```

**Иконка:** ↑ (ArrowUpFromLine)  
**Цвет:** Красный

---

### 3. Оплата встречи (booking_payment)
**Описание:** Мужчина оплачивает бронирование

**Когда создается:**
- Девушка подтверждает запрос
- Деньги списываются с баланса мужчины на эскроу

**Атрибуты:**
```typescript
{
  type: 'booking_payment',
  amount: 25000,
  currency: 'RUB',
  status: 'completed',
  description: 'Оплата встречи с Анастасия',
  relatedBookingId: 123,
  toUser: 'Анастасия'
}
```

**Иконка:** 💳 (CreditCard)  
**Цвет:** Синий

---

### 4. Возврат средств (booking_refund)
**Описание:** Возврат денег мужчине

**Когда создается:**
- Девушка отклоняет запрос
- Бронирование отменено до начала
- Технический сбой

**Атрибуты:**
```typescript
{
  type: 'booking_refund',
  amount: 25000,
  currency: 'RUB',
  status: 'completed',
  description: 'Возврат средств: девушка отклонила запрос',
  relatedBookingId: 123
}
```

**Иконка:** 🔄 (RefreshCw)  
**Цвет:** Голубой

---

### 5. Получение оплаты (booking_received)
**Описание:** Девушка получает деньги после встречи

**Когда создается:**
- Встреча успешно завершена
- С эскроу переводится на счет девушки
- Удерживается комиссия платформы

**Атрибуты:**
```typescript
{
  type: 'booking_received',
  amount: 22500, // За вычетом комиссии
  currency: 'RUB',
  status: 'completed',
  description: 'Получение оплаты от Михаил',
  relatedBookingId: 123,
  fromUser: 'Михаил',
  fee: 2500 // 10% комиссия
}
```

**Иконка:** 💵 (DollarSign)  
**Цвет:** Зеленый

---

### 6. Продление встречи (booking_extend)
**Описание:** Мужчина продлевает активную встречу

**Когда создается:**
- Во время встречи нажата кнопка "Продлить"
- Деньги списываются мгновенно

**Атрибуты:**
```typescript
{
  type: 'booking_extend',
  amount: 25000,
  currency: 'RUB',
  status: 'completed',
  description: 'Продление встречи на 1 ч с Анастасия',
  relatedBookingId: 123,
  toUser: 'Анастасия'
}
```

**Иконка:** ⏰ (Clock)  
**Цвет:** Фиолетовый

---

### 7. Оплата VIP (vip_payment)
**Описание:** Покупка/продление VIP статуса

**Когда создается:**
- Пользователь оплачивает VIP через `VIPUpgradeModal`

**Атрибуты:**
```typescript
{
  type: 'vip_payment',
  amount: 999,
  currency: 'RUB',
  status: 'completed',
  description: 'Оплата VIP статуса: 1 месяц'
}
```

**Иконка:** 👑 (Crown)  
**Цвет:** Золотой

---

### 8. Чаевые отправлены (tip_sent)
**Описание:** Мужчина отправил чаевые девушке

**Атрибуты:**
```typescript
{
  type: 'tip_sent',
  amount: 5000,
  currency: 'RUB',
  status: 'completed',
  description: 'Чаевые для Анастасия',
  toUser: 'Анастасия'
}
```

**Иконка:** 💖 (Heart)  
**Цвет:** Розовый

---

### 9. Чаевые получены (tip_received)
**Описание:** Девушка получила чаевые

**Атрибуты:**
```typescript
{
  type: 'tip_received',
  amount: 5000,
  currency: 'RUB',
  status: 'completed',
  description: 'Чаевые от Михаил',
  fromUser: 'Михаил'
}
```

**Иконка:** 🎁 (Gift)  
**Цвет:** Розовый

---

## Статусы транзакций

| Статус | Описание | Цвет бейджа |
|--------|----------|-------------|
| `pending` | В обработке, ожидает завершения | Желтый |
| `completed` | Успешно завершено | Зеленый |
| `failed` | Не удалось выполнить | Красный |
| `cancelled` | Отменено пользователем | Серый |

---

## Компонент TransactionHistory

### Основные функции:

#### 1. Фильтрация
**По типу операции:**
- Все операции
- Пополнения
- Выводы
- Оплата встреч
- Получение оплаты
- Продления
- VIP оплаты

**По валюте:**
- Все валюты
- RUB, USD, EUR
- BTC, ETH, USDT

**По статусу:**
- Все статусы
- Завершено
- В обработке
- Не удалось
- Отменено

#### 2. Статистика
В заголовке отображается:
- 💚 **Поступления:** сумма всех входящих операций
- ❤️ **Расходы:** сумма всех исходящих операций

#### 3. Отображение
Каждая транзакция показывает:
- Иконка типа операции (цветная)
- Название и статус (бейдж)
- Описание
- От кого / Кому (если применимо)
- Дата и время
- Сумма (зеленая +/красная -)
- Комиссия (если была)

---

## Утилиты transactionManager.ts

### Создание транзакций:

```typescript
// Пополнение
createDepositTransaction(amount, currency)

// Вывод
createWithdrawTransaction(amount, currency, address)

// Оплата встречи
createBookingPaymentTransaction(bookingId, amount, currency, sellerName)

// Возврат
createBookingRefundTransaction(bookingId, amount, currency, reason)

// Получение оплаты (продавец)
createBookingReceivedTransaction(bookingId, amount, currency, buyerName, fee)

// Продление
createBookingExtendTransaction(bookingId, amount, currency, hours, sellerName)

// VIP
createVIPPaymentTransaction(amount, currency, planName)

// Чаевые
createTipSentTransaction(amount, currency, sellerName)
createTipReceivedTransaction(amount, currency, buyerName)
```

### Изменение статуса:

```typescript
completeTransaction(transaction)  // → completed
failTransaction(transaction)      // → failed
cancelTransaction(transaction)    // → cancelled
```

### Аналитика:

```typescript
// Фильтрация по дате
filterTransactionsByDate(transactions, startDate, endDate)

// Расчет баланса из транзакций
calculateBalanceFromTransactions(transactions, currency, initialBalance)

// Статистика
getTransactionStats(transactions)
// Возвращает:
// {
//   totalIncome,
//   totalExpense,
//   totalFees,
//   netBalance,
//   transactionCount
// }
```

### Экспорт:

```typescript
exportTransactionsToCSV(transactions)
// Возвращает CSV-строку для скачивания
```

---

## Интеграция в профиль

### Вкладки в профиле:

1. **💰 Кошелек**
   - `WalletCard` - балансы
   - `WorkScheduleManager` - график (продавцы)

2. **📜 Транзакции** ← НОВАЯ
   - `TransactionHistory` - полная история
   - Фильтры и статистика
   - Автообновление при операциях

3. **📅 Бронирования**
   - История встреч
   - Активные бронирования

---

## Автоматическое создание транзакций

### При пополнении:
```typescript
const handleDeposit = (currency, amount) => {
  const transaction = createDepositTransaction(amount, currency);
  setTransactions(prev => [transaction, ...prev]);
  
  // Обновить баланс
  updateWalletBalance(currency, amount);
};
```

### При выводе:
```typescript
const handleWithdraw = (currency, amount, address) => {
  const transaction = createWithdrawTransaction(amount, currency, address);
  setTransactions(prev => [transaction, ...prev]);
  
  // Уменьшить баланс
  updateWalletBalance(currency, -amount);
};
```

### При оплате встречи:
```typescript
const handleBookingConfirm = (booking) => {
  // Транзакция для покупателя
  const paymentTx = createBookingPaymentTransaction(
    booking.id,
    booking.totalPrice,
    booking.currency,
    booking.sellerName
  );
  
  // Списать с баланса покупателя
  buyerTransactions.add(paymentTx);
  buyerWallet.subtract(booking.totalPrice);
  
  // Заморозить на эскроу
  escrow.lock(booking.totalPrice);
};
```

### При завершении встречи:
```typescript
const handleBookingComplete = (booking) => {
  const fee = booking.totalPrice * 0.1; // 10%
  const sellerAmount = booking.totalPrice - fee;
  
  // Транзакция для продавца
  const receivedTx = createBookingReceivedTransaction(
    booking.id,
    sellerAmount,
    booking.currency,
    booking.buyerName,
    fee
  );
  
  // Перевести продавцу
  sellerTransactions.add(receivedTx);
  sellerWallet.add(sellerAmount);
  
  // Снять с эскроу
  escrow.release(booking.id);
};
```

---

## Примеры использования

### Отображение истории:
```tsx
<TransactionHistory transactions={transactions} />
```

### С вкладками:
```tsx
<Tabs defaultValue="wallet">
  <TabsList>
    <TabsTrigger value="wallet">Кошелек</TabsTrigger>
    <TabsTrigger value="transactions">Транзакции</TabsTrigger>
  </TabsList>
  
  <TabsContent value="wallet">
    <WalletCard wallet={wallet} />
  </TabsContent>
  
  <TabsContent value="transactions">
    <TransactionHistory transactions={transactions} />
  </TabsContent>
</Tabs>
```

---

## Дизайн и UX

### Цветовая схема:
- 🟢 Зеленый - входящие (deposit, refund, received, tip_received)
- 🔴 Красный - исходящие (withdraw, payment, extend, vip, tip_sent)
- 🟡 Желтый - в обработке (pending)
- ⚫ Серый - завершенные/отмененные

### Адаптивность:
- Desktop: 3 колонки фильтров
- Mobile: 1 колонка фильтров
- Карточки транзакций адаптируются

### Производительность:
- Виртуализация для больших списков
- Пагинация ("Загрузить еще")
- Локальная фильтрация (без запросов)

---

## Безопасность

### Не показываем:
- Полные номера карт
- Полные адреса кошельков
- Персональные данные других пользователей

### Показываем:
- Первые 4 и последние 4 цифры карты
- Первые 10 символов адреса + "..."
- Только имена пользователей

### Логирование:
- Все операции записываются
- Невозможно удалить транзакции
- Только добавление новых

---

## Будущие улучшения

- [ ] Экспорт в PDF/Excel
- [ ] Email уведомления о транзакциях
- [ ] Графики доходов/расходов
- [ ] Категоризация расходов
- [ ] Бюджеты и лимиты
- [ ] Рекуррентные платежи
- [ ] Мультиподписи для крупных сумм

---

Полная система учета финансов готова! 🎉