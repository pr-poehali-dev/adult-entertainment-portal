# Аудио компоненты

Компоненты для работы с аудио-приветствиями в объявлениях.

## AudioRecorder

Компонент для записи и загрузки аудио-приветствий.

### Возможности:
- Запись аудио через микрофон (макс. 20 секунд)
- Загрузка готовых аудио файлов
- Пауза/продолжение записи
- Предпросмотр записи
- Удаление и перезапись

### Пример использования:
```tsx
import { AudioRecorder } from '@/components/audio/AudioRecorder';

<AudioRecorder 
  onRecordingComplete={(blob, duration) => {
    // Обработка записанного аудио
    console.log('Audio recorded:', blob, duration);
  }}
  maxDuration={20}
  existingAudioUrl={audioUrl}
/>
```

## AudioPlayer

Компонент для воспроизведения аудио-приветствий.

### Возможности:
- Воспроизведение/пауза
- Прогресс-бар с возможностью перемотки
- Отображение времени

### Пример использования:
```tsx
import { AudioPlayer } from '@/components/audio/AudioPlayer';

<AudioPlayer 
  audioUrl="https://example.com/audio.mp3"
  duration={15}
/>
```

## Интеграция в объявления

Аудио-приветствия доступны только для платных объявлений (50 💗).

### Где используется:
- `CreateAdModal` - форма создания объявления с возможностью записи аудио
- `MyAdsPage` - отображение аудио в своих объявлениях
- `AdRequestCard` - отображение аудио в каталоге объявлений

### Бейджик "С голосом":
Объявления с аудио-приветствием помечаются специальным фиолетовым бейджем с иконкой Volume2.
